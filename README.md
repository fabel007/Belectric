# Welcome site (Belectric)

This is a tiny static welcome page with a happy image.

To view locally:

```bash
cd /workspaces/Belectric
python3 -m http.server 8000
# then open http://localhost:8000 in your browser
```
